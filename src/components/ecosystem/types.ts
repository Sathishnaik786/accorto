export interface Partner {
  id: string;
  name: string;
  glowColor: string;
}

export interface Dimensions {
  width: number;
  height: number;
  radius: number;
  centerX: number;
  centerY: number;
  nodeWidth: number;
  nodeHeight: number;
  centerNodeSize: number;
}
